/**
 * entry file
 *
 * @author wujohns
 * @date 18/12/31
 */
'use strict'

import Consts from './constants'
import Utils from './utils_back'
import Easings from './easings'

const anime = (params = {}) => {
    let now, startTime, lastTime = 0;
    let resolve = null;

    
};
